name: Deploy to Azure VM

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          script: |
            cd ~/sustainomics
            git pull origin main
            pnpm install --frozen-lockfile
            pnpm build
            # Seed on first deploy only (data.db absent)
            if [ ! -f data.db ]; then
              npx emdash seed seed/seed.json --on-conflict=update
            fi
            # Always run — idempotent, adds any new magazines/media not yet in DB
            node scripts/setup-magazines.mjs
            pm2 restart sustainomics
